// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
    provider      = "prisma-client-js"
    binaryTargets = ["native", "linux-musl"]
}
datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

// model Groceries_list{
//   id     String  @id @default(auto()) @map("_id") @db.ObjectId
//   name        String
//   createdAt   DateTime @default(now())
//   updatedAt   DateTime @updatedAt
//   done        Boolean @default(false)
//   items       Item[]
//   index_items Int[]
//   index_items_done Int[]
// }

// model Inventory {
//   id        String   @id @default(auto()) @map("_id") @db.ObjectId
//   name      String
//   createdAt DateTime @default(now())
//   updatedAt DateTime @updatedAt
//   items     Item[]   
//   index_items Int[]
// }

model Item {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  name      String
  auto      Boolean  @default(false)
  //quantity  Float
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  // category  Category
  // place     Place
  // list      Groceries_list? @relation(fields: [list], references: [id])
  // g_list   Groceries_list   @relation(fields: [list_id], references: [id])
  // list_id   String @db.ObjectId // relation scalar field  (used in the `@relation` attribute above)
  // i_list   Inventory   @relation(fields: [inv_id], references: [id])
  // inv_id   String @db.ObjectId // relation scalar field  (used in the `@relation` attribute above)
  // recipe   Recipe   @relation(fields: [inv_id], references: [id])
  // rec_id   String @db.ObjectId // relation scalar field  (used in the `@relation` attribute above)
}

// model Recipe {
//   id        String   @id @default(auto()) @map("_id") @db.ObjectId
//   name      String
//   time      Float
//   difficulty Int
//   createdAt DateTime @default(now())
//   updatedAt DateTime @updatedAt
//   ingredients Item[]
//   instructions String
// }

// enum Place {
//   FRIDGE
//   BATHROOM
//   INVENTORY
//   OTHER
// }

// enum Category {
//   FRUIT
//   VEGETABLE
//   MEAT
//   FISH
//   DAIRY
//   BAKERY
//   PANTRY
//   FROZEN
//   HYGIENE
//   CLEANING
//   OTHER
// }
